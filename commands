#include "monty.h"
/**
 * push - add node to list
 *@txt: string to push
 *@sep: separator
 * Return: pointer to words.
 */
int push(char *opcode, void (*f))
{
	add_dnodeint(dlistint_t **head, const int n);
	return (ptr);
}
